Delete this file before we merge back into main

- let's talk about `ingest_parsed_documents`. The implementation is good, but the reasons why are subtle so worth looking at
- the shift from doc_parser to response_parser is big, but should be quite straightforward with claude if we get the prompt right I think. We can go through that tomorrow
