"""add processing_id to dataset

Revision ID: dfd25558499d
Revises: ce918ee2acb3
Create Date: 2026-08-04 13:42:11.620035

"""

from __future__ import annotations

from collections.abc import Sequence

import sqlalchemy as sa

# SQLModel's string columns autogenerate as `sqlmodel.sql.sqltypes.AutoString`,
# so migration scripts need this importable even though it looks unused.
# The submodule is imported explicitly (rather than just `import sqlmodel`)
# so that type checkers can see where `AutoString` comes from.
import sqlmodel.sql.sqltypes
from alembic import op

revision: str = "dfd25558499d"
down_revision: str | None = "ce918ee2acb3"
branch_labels: str | Sequence[str] | None = None
depends_on: str | Sequence[str] | None = None


def upgrade() -> None:
    """Apply this migration"""
    # ### commands auto generated by Alembic - please adjust! ###
    with op.batch_alter_table("dataset", schema=None) as batch_op:
        batch_op.add_column(
            sa.Column(
                "processing_id", sqlmodel.sql.sqltypes.AutoString(), nullable=False
            )
        )
        batch_op.drop_constraint(batch_op.f("uq_dataset_facets"), type_="unique")
        batch_op.create_unique_constraint(
            "uq_dataset_facets",
            [
                "variable",
                "processing_id",
                "experiment",
                "model",
                "variant_label",
                "reporting_interval",
                "grid_label",
                "project",
                "institution",
            ],
        )

    # ### end Alembic commands ###


def downgrade() -> None:
    """Undo this migration"""
    # ### commands auto generated by Alembic - please adjust! ###
    with op.batch_alter_table("dataset", schema=None) as batch_op:
        batch_op.drop_constraint("uq_dataset_facets", type_="unique")
        batch_op.create_unique_constraint(
            batch_op.f("uq_dataset_facets"),
            [
                "variable",
                "experiment",
                "model",
                "variant_label",
                "reporting_interval",
                "grid_label",
                "project",
                "institution",
            ],
        )
        batch_op.drop_column("processing_id")

    # ### end Alembic commands ###
