Made saving search results to the database thread safe (needed to run a search in parallel). If two workers enter a race (e.g. try to write the same dataset to the database at once), the 'second' worker uses the entry written by the 'first' worker rather than raising an error.
