Added saving of search results to the database (see [esmporium.db.Dataset][]). Added [esmporium.search.search_api_facade.result_parsers][] to explicitly support parsing of results, because the result shape depends not only on the API, but also the project and sometimes even the host too.
