 Added pagination so that a single search query now fetches every matching record, paging through the results automatically when more match than fit in one page (set the page size with `limit`). When a search is large enough to page through more than one requests, it first emits a [PaginationWarning][esmporium.search.PaginationWarning] so a user knows it may take a while.
