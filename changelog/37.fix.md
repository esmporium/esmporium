Facet names supplied via `other_terms` in [esmporium.query] are now sent on the search request. Previously, they were silently dropped when a query was rendered to [esmporium.search.search_api_facade.parameters][].
